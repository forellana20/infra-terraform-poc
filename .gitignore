# Exclude the .terraform directory where providers and plugins are stored
.terraform/

# Exclude Terraform state files that contain sensitive information
*.tfstate
*.tfstate.backup

# Exclude any crash logs or core dumps generated by Terraform
crash.log
*.log

# Exclude plan output files (e.g., terraform plan -out)
*.tfplan

# Exclude Terraform override files (commonly used for local setups)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Exclude backup files created by editors
*~
